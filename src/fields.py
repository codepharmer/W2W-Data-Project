import time
import datetime
from datetime import date
#START_DATE = str(date.today()-datetime.timedelta(days=1)) #first day to be included, 'YYYY-MM-DD'
START_DATE = str(date.today())
END_DATE = str(date.today()) #last day to be included, 'YYYY-MM-DD'
OUTPUT_DIRECTORY = '/Users/Wesso/Desktop/W2WScrapingData/'